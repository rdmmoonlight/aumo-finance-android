<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AumoFinance.Pages.Auth.LoginPage"
             BackgroundColor="#35203F"
             NavigationPage.HasNavigationBar="False"
             Title="Log In">

    <Grid VerticalOptions="Center" Padding="24">

        <!-- Login Card Container -->
        <Border BackgroundColor="#42284F"
                Stroke="#4A2E59"
                StrokeThickness="1"
                StrokeShape="RoundRectangle 20"
                Padding="28,32">

            <VerticalStackLayout Spacing="20">

                <!-- Header & App Logo/Icon -->
                <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                    <Border BackgroundColor="#42284F"
                            Stroke="#5A3F6B"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 18"
                            HeightRequest="60"
                            WidthRequest="60"
                            HorizontalOptions="Center"
                            Margin="0,0,0,8">
                        <Image Source="applogo.png"
                               HeightRequest="45"
                               WidthRequest="45"
                               Aspect="AspectFit"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Border>

                    <Label Text="Aumo Finance"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="#FFFFFF"
                           HorizontalTextAlignment="Center"
                           CharacterSpacing="0.5" />

                    <Label Text="Log in to your account to continue"
                           FontSize="13"
                           TextColor="#E8E2EA"
                           HorizontalTextAlignment="Center" />
                </VerticalStackLayout>

                <!-- Alert/Error Box -->
                <Border x:Name="ErrorCard"
                        IsVisible="False"
                        BackgroundColor="#402531"
                        Stroke="#6B3F47"
                        StrokeThickness="1"
                        StrokeShape="RoundRectangle 12"
                        Padding="12,10">
                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="10" VerticalOptions="Center">
                        <Label Grid.Column="0"
                               Text="&#xea05;"
                               FontFamily="TablerIcons"
                               TextColor="#D05A63"
                               FontSize="16"
                               VerticalOptions="Center" />
                        <Label x:Name="ErrorText"
                               Grid.Column="1"
                               Text="Invalid email or password."
                               TextColor="#E8E2EA"
                               FontSize="12"
                               VerticalOptions="Center" />
                    </Grid>
                </Border>

                <!-- Input Form Wrapper -->
                <VerticalStackLayout Spacing="16">

                    <!-- Email Input -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Email"
                               TextColor="#E8E2EA"
                               FontSize="12"
                               FontAttributes="Bold" />

                        <Border BackgroundColor="#2A1930"
                                Stroke="#4A2E59"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                Padding="14,2">
                            <Entry x:Name="EmailEntry"
                                   Placeholder="name@email.com"
                                   PlaceholderColor="#9C8FA6"
                                   TextColor="#FFFFFF"
                                   FontSize="14"
                                   Keyboard="Email"
                                   ReturnType="Next"
                                   HeightRequest="44" />
                        </Border>
                    </VerticalStackLayout>

                    <!-- Password Input -->
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Password"
                               TextColor="#E8E2EA"
                               FontSize="12"
                               FontAttributes="Bold" />

                        <Border BackgroundColor="#2A1930"
                                Stroke="#4A2E59"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                Padding="14,2">
                            <Grid ColumnDefinitions="*, Auto">
                                <Entry x:Name="PasswordEntry"
                                       Grid.Column="0"
                                       Placeholder="••••••••"
                                       PlaceholderColor="#9C8FA6"
                                       TextColor="#FFFFFF"
                                       FontSize="14"
                                       IsPassword="True"
                                       ReturnType="Done"
                                       HeightRequest="44"
                                       VerticalOptions="Center"
                                       Completed="OnLoginButtonClicked" />

                                <Button x:Name="TogglePasswordButton"
                                        Grid.Column="1"
                                        Text="&#xecf0;"
                                        FontFamily="TablerIcons"
                                        BackgroundColor="Transparent"
                                        TextColor="#E8E2EA"
                                        FontSize="20"
                                        Padding="0"
                                        BorderWidth="0"
                                        CornerRadius="0"
                                        HeightRequest="40"
                                        WidthRequest="40"
                                        VerticalOptions="Center"
                                        Clicked="OnTogglePasswordClicked" />
                            </Grid>
                        </Border>
                    </VerticalStackLayout>

                    <!-- Remember Me Checkbox -->
                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="8" VerticalOptions="Center">
                        <CheckBox x:Name="RememberMeCheckBox"
                                  Grid.Column="0"
                                  Color="#523363"
                                  VerticalOptions="Center" />
                        <Label Grid.Column="1"
                               Text="Remember me"
                               TextColor="#E8E2EA"
                               FontSize="13"
                               VerticalOptions="Center">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnRememberMeLabelTapped" />
                            </Label.GestureRecognizers>
                        </Label>
                    </Grid>

                </VerticalStackLayout>

                <!-- Login Button, Biometrics & Indicator -->
                <VerticalStackLayout Spacing="12" Margin="0,4,0,0">
                    <ActivityIndicator x:Name="LoadingIndicator"
                                       Color="#8B6C9C"
                                       IsRunning="False"
                                       IsVisible="False"
                                       HeightRequest="36" />

                    <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                        <Button x:Name="LoginButton"
                                Grid.Column="0"
                                Text="Log In"
                                Clicked="OnLoginButtonClicked"
                                BackgroundColor="#523363"
                                TextColor="#FFFFFF"
                                FontAttributes="Bold"
                                FontSize="15"
                                CornerRadius="12"
                                HeightRequest="48" />

                        <!-- Biometric Login Button -->
                        <Button x:Name="BiometricButton"
                                Grid.Column="1"
                                Text="&#xebd1;"
                                FontFamily="TablerIcons"
                                FontSize="22"
                                BackgroundColor="#42284F"
                                TextColor="#E8E2EA"
                                BorderColor="#5A3F6B"
                                BorderWidth="1"
                                CornerRadius="12"
                                HeightRequest="48"
                                WidthRequest="48"
                                IsVisible="False"
                                Clicked="OnBiometricButtonClicked" />
                    </Grid>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </Border>

    </Grid>

</ContentPage>
