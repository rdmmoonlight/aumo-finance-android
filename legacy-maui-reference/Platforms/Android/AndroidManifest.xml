<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

	<!-- App dikunci penuh ke Android 9 (API 28): min = target = max = 28.
	     Ini memastikan tidak ada perilaku/permission API 29+ yang pernah
	     aktif, dan APK tidak bisa terpasang di device di atas Android 9. -->
	<uses-sdk android:minSdkVersion="28" android:targetSdkVersion="28" android:maxSdkVersion="28" />

	<!-- Biometric Authentication Permission -->
	<uses-permission android:name="android.permission.USE_BIOMETRIC" />

	<!-- Network Permissions -->
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- APK Update Installation Permission (Android 8.0+) -->
	<uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

	<application 
		android:allowBackup="true" 
		android:icon="@mipmap/appicon" 
		android:roundIcon="@mipmap/appicon_round" 
		android:supportsRtl="true"
		android:usesCleartextTraffic="true">

		<!-- FileProvider for APK Sharing to Package Installer -->
		<provider
			android:name="androidx.core.content.FileProvider"
			android:authorities="${applicationId}.fileprovider"
			android:exported="false"
			android:grantUriPermissions="true">
			<meta-data
				android:name="android.support.FILE_PROVIDER_PATHS"
				android:resource="@xml/file_paths" />
		</provider>

		<!-- Disable androidx.emoji2 auto-init: prevents EmojiTextWatcher from
		     attaching to numeric-keyboard Entry fields, which causes
		     "end should be < than charSequence length" crash in EmojiCompat.process -->
		<provider
			android:name="androidx.startup.InitializationProvider"
			android:authorities="${applicationId}.androidx-startup"
			android:exported="false"
			tools:node="merge">
			<meta-data
				android:name="androidx.emoji2.text.EmojiCompatInitializer"
				tools:node="remove" />
		</provider>

	</application>

</manifest>
